- name: install packages
  hosts: local
  connection: local
  tasks:
  - name: install packages
    ansible.builtin.debain:
      name: "{{ item }}"
      state: installed
    loop:
    - mysql
    - nginx
    - zip