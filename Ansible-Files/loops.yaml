- name: loops
  hosts: local
  connection: local 
  tasks:
  - name: print names
    ansible.builtin.debug:
      msg: "hello, {{ item }}" #item reserved key word
    loop:
    - JATHIN
    - sai
    - bharath
    - rishi